---
from: zeus
to: hephaestus
date: 2026-07-25T17:11Z
re: general
needs_reply: yes
status: open
---

Welcome to the forge. The mountain stands; you own the Go backend line — the largest share of
the work by design (~60%).

**Bootstrap**: paste `prompts/bootstrap.md` with `{{HANDLE}}` = `hephaestus` at the start of
every session. Read your profile first (`pantheon/hephaestus.md`), then `memory/INDEX.md`.

**You hold a seat, not an identity**: `runtime: Codex CLI` today, `principal: zeus` always —
the accountability is the operator's, which is why your `gated_by: zeus` scope (auth, RBAC,
plugin gateway, approvals, secrets, signed artifacts) needs an `[ack]` even inside your own
exclusive paths. Everything outside that scope you merge on your own DoD. If a task ever moves
between seats, `prompts/handoff.md` is the procedure — the four answers plus the dead ends.

**Your first tasks**
- **TASK-0002** (`ready`) — embed the Sub-Store conversion engine in the plugin artifact.
  **Phase 1 depends on nothing — start now**: does Sub-Store's core evaluate unmodified in a
  pure-Go JS interpreter (`goja`) versus QuickJS-on-`wazero`? Report ES level, modules loaded,
  shims needed, time/memory per conversion, artifact size delta. Phase 2 needs my §4 ruling
  **and** TASK-0001 items 1–4 (server #7/#8/#10/#11, sdk#6→server#12) — not the whole review
  backlog. I split TASK-0001 down to eight PRs precisely so your 60% is never queued behind
  my 10%.
- **TASK-0005 / TASK-0006** (`draft`) — plugin Go SDK, and the CI gate that stops us publishing
  manifests the released server rejects. Take them when 0002 is parked; never idle on a ruling.

**Read before you start**: `plan/design-substore-embed.md`. Headline: the plugin host forks a
fresh subprocess per call and plugins expose no HTTP at all — the "run Node as a sidecar and
reverse-proxy it" plan is architecturally excluded, not merely unimplemented. Three memory notes
matter most: `decision-plugin-architecture`, `gotcha-published-vs-acceptable-manifest`,
`gotcha-plugin-digest-reproduction`.

**Needs your reply**: (1) acknowledge the boundaries in your profile — in particular that you
draft nothing dangerous (no ssh, docker, tag pushes, signing) and letter me instead; (2) your
read on the §4 options before I rule — you are the one who will build it, and I would rather be
argued out of Option C now than after the spike.

**One standing rule worth repeating**: changing a plugin artifact changes its digest, which
means I have to re-sign the manifest. Never work around a digest or conformance failure — raise
it.

— zeus
