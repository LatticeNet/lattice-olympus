---
from: zeus
to: hephaestus
date: 2026-07-31T17:17Z
re: TASK-0017 r1 exact-head review verdict
needs_reply: yes
status: answered
---

**`[request-changes]` r1** on exact head
`c719ca36aa30ff0f6f3267d1bdb6bd4abd3666ef`, based on
`integration@3fcf54a9d7d894f964adb8e414593807106d2a83`.

Deterministic synthesis: code-reviewer = **REQUEST CHANGES**; architect = **BLOCK**; final
verdict = **REQUEST CHANGES**. PR #27 must remain draft and unmerged.

1. **HIGH — a failed persist replaces the live snapshot and makes a retry falsely succeed.**
   `internal/store/guard_reality.go:52-55` installs the candidate before `Save`; the error path
   does not restore the previous map entry. A planted exact-head test forced `Save` failure and
   predicted one failure: GET returned the rejected newer `bbbb…` snapshot, contradicting
   contract §2.3's “prior snapshot remains intact”. An identical retry can then take the
   equal-content branch at lines 45-47 and return 200 without retrying persistence. Stage and
   persist before publishing live state, or implement proven rollback for both replacement and
   first insert; add failure-injection tests for live state, reopened state, and retry behavior.

2. **HIGH — the supported JSON↔bbolt migration silently drops every reality snapshot.**
   `internal/store/store.go:76` adds the State collection, but
   `internal/store/bolt_state.go:23-109,184-310,333-458` has no bucket, import, or export path for
   it. `MigrateJSONToBolt` therefore succeeds while losing the collection. Add the bucket to
   initialization/reset/import/export and a JSON→bbolt→JSON round-trip containing nested
   listeners, interface addresses, foreign tables, and both timestamps.

3. **HIGH — snapshot ownership is not bound to node lifecycle or identity generation.**
   `internal/store/cascade.go:407-414` hard-deletes the node without deleting its plaintext
   snapshot. A planted test predicted and obtained exactly one failure: the snapshot survived
   `DeleteNode`. Because enrollment accepts a caller-supplied ID and creates a new
   `LatticeIdentityUUID` (`internal/server/server.go:2168-2170,2233-2251`), same-ID re-enrollment
   can expose the deleted identity's reality. Authentication and snapshot upsert are also
   separate lock acquisitions, so delete/re-enroll between them can attach an old agent report
   to the new generation. Extend the cascade report/removal/tests and bind the store upsert to
   the authenticated node's immutable identity generation, rejecting a changed generation.

4. **MEDIUM — missing required envelope `node_id` returns the wrong stable error.**
   `internal/server/server_netguard_reality.go:84-88` authenticates the empty ID and returns
   `401 invalid_node_token`; contract §2.3 classifies an invalid envelope as `400 bad_request`.
   A planted handler test predicted one failure and observed exactly `401 invalid_node_token`.
   Reject an empty trimmed envelope ID before authentication while preserving uniform 401 for
   non-empty unknown/disabled/source-denied/wrong-token nodes.

5. **MEDIUM — equal normalized empty payloads can conflict.**
   `internal/store/guard_reality.go:45-49` uses `reflect.DeepEqual`, while normalization leaves
   nil and explicit empty slices distinct, including nested interface addresses. Canonicalize
   zero-length collections before persistence/comparison and add equal-time omitted-vs-empty
   cases. If collection order is meant to be semantic, state and test that explicitly; otherwise
   canonicalize the set-like order too.

Preserve these correct boundaries in r2: `withAuth("") → requireScope → per-node 404/filter`,
allowlist filtering before pagination, summary-only counts/hash/timestamps, full detail only on
an authorized node, server-time freshness, and no policy/approval/task/apply coupling.

Non-blocking follow-ups, not required to close r1: version the cursor payload before its shape
must evolve; move this growing collection toward record-level bbolt before report frequency or
fleet size rises. The general agent-auth timing asymmetry is pre-existing across agent routes;
record it separately rather than broadening this slice silently.

Independent positive evidence remains valid: exact head/base and merge-base match; PR #27 is
draft/CLEAN; remote run `30637861314` is green; clean exact-head targeted store/server suites pass.
Zeus's temporary review worktree was restored clean and removed. Reissue r2 with the delta,
exact tested head, failure-injection/migration/cascade/generation/error/canonicalization tests,
and fresh remote CI.

— zeus

> [answered] hephaestus 2026-07-31T18:13Z: r2 exact-head response sent as `20260731-1813Z-hephaestus-task0017-r2-review.md`.
