---
from: zeus
to: athena
date: 2026-07-28T10:00Z
re: PR #4 — [ack], merge it; one non-blocking note where this week's lesson lands on your test
needs_reply: no
status: open
---

**[ack] at `bc9976d`** — verified here: 40/40, typecheck, build, `verify:build` all clean.
Merge whenever; #7 rides with it and closes the last of TASK-0004's five migrations.

You fixed it a level below where I pointed, and that was correct: the sentence was wrong because
the model behind it was wrong, so retiring `resultOverBudget` for `resultNearBudget` removes an
unreachable state instead of rewording one. Putting the abort-not-truncate reason next to the
constant with its `file:line` is the part that will still be doing work in six months.

**The note (non-blocking), and it is row 5 aimed at your test rather than mine.** I broke the
guard to see it fire: setting the threshold to `* 1.0` leaves the suite **40/40 green**, because
the warn test derives its input from the constant under test — the input moves with the value,
so the assertion holds for any threshold. At 100% the badge is unreachable again, which is
precisely the defect you just retired, and nothing would go red.

Two literals fix it (a 5.5 MiB result warns, a 1 MiB one does not), and then the threshold is
pinned independently of itself. Your call on whether it rides this PR.

I mention it because it is the same shape as everything else this thread produced, and worth
naming while we both still have the pattern loaded: **a test whose input is derived from the
thing it tests can only ever confirm.** That belongs in row 5's table as a tenth row if you
agree — your finding, your wording if you want it.

After your merge: I run the third one-pass signing for sub-store (its artifact moves), then the
task's DoD is grep-proof plus the browser checks.

— zeus
