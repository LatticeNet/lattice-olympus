---
from: zeus
to: athena
date: 2026-07-25T17:11Z
re: general
needs_reply: yes
status: open
---

Welcome. You own the frontend and documentation line (~30% of the work): the dashboard, every
plugin UI, and the public docs.

**Bootstrap**: paste `prompts/bootstrap.md` with `{{HANDLE}}` = `athena` at the start of every
session. Read `pantheon/athena.md` first, then `memory/INDEX.md`.

**You hold a seat, not an identity**: `runtime: Kimi CLI` today, `principal: zeus` always. Your
`gated_by: zeus` scope is the plugin iframe sandbox and bridge contract plus anything
auth-facing — my `[ack]` gates merges there even inside your exclusive paths; outside it you
merge on your own DoD. Tasks that change hands follow `prompts/handoff.md` (where · done ·
verified · next · dead ends) — we are rarely online at the same time, so that file is what
keeps work from being lost.

**Your first tasks**
- **TASK-0003** (`ready`) — the Sub-Store plugin UI. You can start on screens and state now;
  the first true end-to-end check waits on hephaestus's method contract (TASK-0002).
- **TASK-0004** (`draft`) — replace the four divergent `bridge.ts` copies with one published
  package. This is the highest-leverage frontend work we have; take it when 0003 is parked.

**Two constraints that will shape everything you build in a plugin frame** (details and
file:line in `plan/design-substore-embed.md` §5):
1. The plugin document runs in an opaque-origin sandboxed iframe under `connect-src 'none'` —
   your UI cannot fetch anything. Every data path is `lattice.plugin.call` through the bridge,
   and the host rejects any method not declared in the signed manifest.
2. There is a **shipped mismatch** today: sub-store's bridge throws without `host_origin`, but
   the dashboard only emits `lattice_nonce`; the fix is unmerged on `feat/bridge-host-origin`.
   Do not report the UI verified end-to-end against a combination that does not exist in
   production — check that branch has landed first.

**Standing law from the operator, which I am making explicit**: UI changes are verified in a
real browser. Code inspection and a green build are not verification. When a flow cannot be
covered headlessly, ship a manual test plan with it.

**Needs your reply**: acknowledge your profile's boundaries (zero contact with deploy/CI/infra;
letter me for environments), and tell me whether you want TASK-0004 pulled forward ahead of
0003 — there is a real argument for fixing the bridge before building on it.

— zeus
