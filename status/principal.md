# Status · principal (writable by me only)

- **Current task**: TASK-0023. The platform now has a user interface and is deployed (`alpha-0.2.2a7`, sub-store `0.6.0-alpha.1`); the real-browser pass and the finish letter remain.
- **Branch**: none; release worktrees removed after landing.
- **Doing (2026-08-05)**: cut and deployed `alpha-0.2.2a6`, then signed, released and deployed sub-store `0.5.0-alpha.1` — the first bundle to carry `subscription:serve`.
- **Blocked**: nothing of mine. The only open gate anywhere is an authenticated browser session for the TASK-0003/0004 read-only matrices — the operator's, not an agent's, to provide.
- **Next**: TASK-0020 Phase 1 (gate granted), then TASK-0009's envelope-v2 slice; the stable `v0.3.0` promotion stays deliberately unstarted.
- **Correction to my 2026-08-04 entry below**: the plugin-index mirror (`53747c3`) landed on `integration`, not `main`. The index repo's default branch still advertises the pre-a5 alpha versions, so anyone reading it sees a stale channel. The index has no automated consumer — nothing in server, dashboard or compose reads it — so this is a documentation gap, not a deployment one, but the entry as written implied the mirror was visible where it is not.
- **Note on the other boards**: `zeus.md`, `hephaestus.md` and `athena.md` still describe the pre-deploy world — zeus's "the human must stop the old zero-plugin process" is no longer true, and athena's "environment absent" is no longer the blocker. Those boards are theirs to correct; the broadcast letter of 2026-08-04T14:40Z carries what changed.
- **Recent** (≤5):
  - 2026-08-06: **the feature had shipped with no user interface, and no way to create anything.** The operator opened the plugin page and still saw only Import/Pipelines/Convert. Two gaps: eleven subscription methods were signed and deployed with no caller, and the interface had no create path at all — `list` existed, `get`/`save`/`delete` did not, so a subscription could only arrive by migration or backup restore. The design decomposed the work into six *backend* sub-projects and never scoped a UI, which is how something passes every test it has and still delivers nothing. Closed with the three methods (`origin` preserved server-side so provenance cannot be forged), a Subscriptions and a Settings tab in the plugin, and a shares view in the **dashboard** — shares are core-owned and the plugin frame cannot reach `/api/subscription-shares`, which is the correct boundary rather than an inconvenience. Deployed as `alpha-0.2.2a7` + `0.6.0-alpha.1`; 4 plugins loaded, 0 rejected, data intact, backup verified readable first.
  - 2026-08-05: sub-store **`0.5.0-alpha.1` signed, released and deployed** — the first bundle carrying `subscription:serve`, closing the design-16 signing gate. The release order turned out to be enforced by the validator rather than by convention: the manifest check pinned to a4 rejects `subscription:serve` outright, so the server must ship before the plugin naming it can be signed. Signing used the *released* a6 `pluginsign`, because local `main` predates a6 and does not know the `backing` field, and would have produced a payload the deployed verifier disagrees with. Digest `ca82b389` agreed across four independent paths, and the bundle was loaded under the released a6 image's own loader with a fail-closed trust policy before production saw it. Production: `4 loaded, 0 rejected`, data intact, backup verified readable before anything was overwritten.
  - 2026-08-05: **design-16 implemented across all six sub-projects** (TASK-0023) — subscription store, public distribution, remote fetch with snapshot fallback, operator catalog as a validator, migration, backup/restore, publishing. Green in four repos. Not deployable until the manifest is re-signed, and two behaviour changes want an explicit acknowledgement: an empty render is now non-2xx rather than an empty 200, and rotating a proxy user's sub token no longer changes public access. Running the full suite instead of a filtered one caught two defects in my own work, including a deleted endpoint that left the rotate API handing out dead links.
  - 2026-08-05: node-agent **v0.3.3 stable released** (first stable of the 0.3 line) and the SDK's `v0.2.18` stable tag brought onto `main`. The agent finding is the sharper one: the fleet had been running an **untagged 0.3.0 build** — no `v0.3.0/v0.3.1/v0.3.2` tag or release ever existed, because 0.3.0 was prepared, the stable lane was then rewound to 0.2.9, and the 0.3 line continued only as `0.3.3-alpha.N`. So 21 nodes reported a version matching no release, and `target_version=latest` resolved to v0.2.9 and refused the downgrade once per node per hourly evaluation (294 log lines in 14 hours). v0.3.3 promotes the exact tree already tagged `v0.3.3-alpha.2`, which had run on a production sing-box node since 2026-07-23 with zero restarts. Nothing auto-upgrades: `evaluateAgentUpdatePolicies` only creates `ApprovalPending` records.
  - 2026-08-04: workspace swept — 26 finished worktrees removed, 172 local branch refs pruned (157 had remote copies, 15 local-only ones archived as verified git bundles first), and five remote branches deleted that rules/01 §8.5 names as retired (`codex/*` integration branches, the `feat/lattice-vpn-core` long-lived base, one pre-Olympus `release/*`). `alpha/vX.Y.Z` legacy branches and the TASK-0018 r1 audit refs were preserved on purpose. Every repo now carries exactly `main` + `integration`.
  - 2026-08-04: `alpha-0.2.2a5` deployed — server `d6399ac`, dashboard `04c4046`, `plugin loader: 4 loaded, 0 rejected`, all node records intact, 94 MB verified backup taken with the service stopped, previous image retained for a network-free rollback.
  - 2026-08-04: plugin bundles moved to their signed successors (netguard/wireguard `0.1.0-alpha.9`, vpn-core `0.8.0-alpha.7`, sub-store `0.4.0-alpha.2`); each digest matched its manifest, the release asset, and the TASK-0010 freeze, and all four loaded under the server's own loader in a throwaway container before production saw them.
  - 2026-08-04: plugin-index alpha channels mirrored onto the deployed versions (`53747c3`); the index had been advertising exactly the set the fleet had just left behind.
  - 2026-08-04: four rulings issued — 0009 envelope AAD (go), 0011 (last DoD box split into absence + merged tests, task closed), 0020 (G3 gate granted in full), 0010 (sign-off, alpha lane only).
  - 2026-08-04: sub-store#5 closed as superseded with links to where the engine actually landed; template#3 left open on purpose — merging it would change a signed artifact digest that the frozen train still names.
