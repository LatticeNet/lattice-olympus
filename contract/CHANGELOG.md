# Contract change log (append-only, never deleted)

One row per change; co-sign = affected members' `[ack]` in the linked letter, then tick the row.

| # | Date (UTC) | Interface/enum/resource | Change | Reason | Impact | Letter | Co-signed |
|---|---|---|---|---|---|---|---|
| 1 | 2026-07-25 | The mountain stands; laws in force | Olympus instantiated: seats zeus / hephaestus / athena (principal · runtime · share · gated_by); integration branch `integration` (legacy `alpha/v*` retired); branch/tag/commit law rules/01 §8.5; public-repo redaction rule AGENTS.md §4; handoff protocol `prompts/handoff.md` | setup | all | — | ☑ (landed on `main` by operator merge) |
| 2 | 2026-07-26 | api-contract §1 · Plugin UI bridge protocol v1 | New section: channel (nonce+host_origin fragment, fail-closed), message enums both directions, inbound guard order, token allowlist, error/timeout/dispose semantics, reload = new trust boundary | TASK-0004 extracts the bridge into `@latticenet/plugin-bridge`; the protocol becomes cross-member surface (athena owns, every plugin UI consumes, zeus stewards) | athena (owner) · hephaestus (informational — engine methods ride `lattice.plugin.call`) | messages/inbox/athena/20260726-1236Z-zeus-bridge-verdict-and-contract.md | ☑ athena `[ack]` 2026-07-26T13:10Z (letter 20260726-1310Z, line-by-line verify) |
| 3 | 2026-07-27 | rules/01 §8.5 · coordinated release train | Train = standalone vX.Y.Z defined by CI-validated train.json (components/pins/verification); plain trains contain no prereleases; min_server signed-manifest-authoritative, index mirrors | Operator ruling 2026-07-26 §1b + delegated Decision B (v0.3.0 standalone, grant record 20260727-0512Z) | all seats | messages/inbox/*/20260727-1035Z-zeus-slice3-amendment.md | ☑ athena 2026-07-27T10:25Z · ☑ hephaestus 2026-07-27T10:4xZ (operator via §1b + delegation) — ROW CO-SIGNED |
| 4 | 2026-07-28 | rules/04 · pre-push redaction check | The check is now a COMMITTED, TESTED SCRIPT (`rules/checks/redaction-scan.sh` + 31-line expectation fixture + harness), not a command in prose: three distinguishable exits (0 clean / 1 findings / **2 scanner broken**) and a self-canary so it cannot fail quietly. Plus the load-bearing clause: **a clean run is not a waiver** — bare aliases/labels in prose are mechanically undetectable and AGENTS.md §4 still binds | Two published drafts were both broken; the second exited 2 printing nothing, i.e. indistinguishable from clean — the exact failure the amendment exists to prevent. Reproduced by athena, then here | all seats (one command in the push habit) | messages/inbox/*/20260728-0730Z-zeus-rules04-r3.md | ☑ athena 2026-07-28T07:09Z (exercised, incl. the canary and a degraded-scanner composition) · ☑ hephaestus 2026-07-28T08:11Z (harness 46/46, exits 0/1/2, CI green) — ROW CO-SIGNED |
| 5 | 2026-07-28 | rules/04 · "Verifying a check: say the number first" | New section: an ENUMERATED TABLE of nine instances (what failed, where the fault lay, what caught it) plus five rules — predict the number before running; treat surprises as the test being wrong; measure exit codes off the pipe; make every check print a count; test guards by breaking what they guard, having confirmed the breakage is real | A thread full of checks reporting success while doing nothing — all caught by a contradicted number, none by re-reading. First draft miscounted itself three ways (audited by athena); replaced by an enumerated table so the count is whatever the record says, which is the section's own subject | all seats (how checks get verified here) | messages/inbox/*/20260728-0855Z-zeus-rules04-verification-section.md | ☑ athena 2026-07-28T07:44Z · ☑ hephaestus 2026-07-28T08:11Z — ROW CO-SIGNED |
| 6 | 2026-07-28 | rules/01 §5.1 · multi-worktree checkout hazard | New: `git checkout <branch>` fails when the branch is checked out in another worktree; a swallowed failure let a following `reset --hard` rewrite the branch the seat was standing on. Prescribes `switch --detach`, forbids `||` fallbacks before destructive commands, requires verifying the target first | Field incident (athena, 2026-07-28) — the thread's tenth failure and the first DESTRUCTIVE one: not a check reporting success while doing nothing, but a destructive command succeeding against the wrong target because its guard failed quietly | all seats (everyone runs multiple worktrees) | messages/inbox/*/20260728-1015Z-zeus-substore-signed-and-worktree-rule.md | ☑ athena 2026-07-28T07:53Z · ☑ hephaestus 2026-07-28T08:11Z — ROW CO-SIGNED |
| 7 | 2026-07-28 | rules/04 · verification table rows 10–11 | Two failure modes earned since the table was signed: a test whose input derives from the constant under test (can only confirm — athena's wording, found by zeus breaking her guard); and an edit script that failed to parse so NOTHING CHANGED while the suite passed honestly on the unchanged state | The second is a new class: every earlier row was a check reporting success while doing nothing; this one is a check reporting success correctly while the WORK did not happen | all seats | messages/inbox/*/20260728-1300Z-zeus-docs-guard-ruling.md | ☐ athena · ☐ hephaestus |
